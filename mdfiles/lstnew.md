lstnew関数の使用に関して
ft_lstnewのmallocでどれだけの領域を確保すべきか、そしてintなどの値をどのように格納する
ft_lstnew関数は「新しいノード」をつくるので、確保するべき領域は「構造体１個分」である。
t_list *node = (t_list *)malloc(sizeof(t_list));

typedef struct s_list
{
	void  		  *content;
	struct s_list *next;
}                 t_list;

t_list *node;
node = (t_list *)malloc(sizeof(t_list));

node.content = content;
node.next = NULL
と
node ->content = content;
node ->next = NULL;
の違い

t_list *node;
と宣言しているため、nodeは構造体t_listそのものではなく、構造体へのポインタである。

| 記号   | 対象                 | 意味                  |
| `.`  | 構造体 **そのもの** に使う   | メンバに直接アクセス          |
| `->` | 構造体 **へのポインタ** に使う | ポインタが指す構造体のメンバにアクセス |

node ->content = contentはなにをしているのか
構造体nodeの中のcontentフィールドに、変数content(つまりnumのアドレス)を代入している。
・contentの中身は「ポインタ」なので、値を直接いれるわけではなく、アドレスをいれている。
・その結果、node ->contentは「データがある場所」を指すようになる
| 式                          | 意味
| `node->content = content;` | `content`の**アドレス（ポインタ）**を入れる |
| `*(int *)node->content`    | そのアドレスが指す**実際の値（例：42）**を取り出す |
「アドレスをつなぐ」とき → node->content = content;
「値を使う」とき　ｰ>　*(int *)node ->content

ft_lstadd_frontaの目的
既存のリストの「先頭」に、新しいノードnewを追加する
イメージ
追加前:  [A] -> [B] -> [C] -> NULL
追加後:  [new] -> [A] -> [B] -> [C] -> NULL

void	ft_lstadd_front(t_list **lst, t_list *new)
{
	new ->next = *lst;
	*lst = new;
}
new->next = *lst;

これはこういう動きです👇

「新しい家（A）の裏庭ドアを、いまの家（B）につなげる」

つまり、「Aの次にBがある」状態にします。

🏡 図でいうと：
A ─→ B ─→ NULL
② *lst = new;

ここが核心です。

「住所メモ（lst）に書かれていた『Bの住所』を消して、
代わりに『Aの住所』を書き直す」

つまり：

lst（＝住所メモ）自体の場所は変わらないけど、

そこに書かれている「住所（ポインタの中身）」が
B → A に書き換わる。

📍ビフォー・アフターのたとえ
🏠 呼び出し前
住所メモ(lst):「Bの家」
Bの家: content="B"
Aの家: content="A"
呼び出し中

new->next = *lst;
Aの家の裏口 → Bの家
*lst = new;
住所メモ(lst)を書き換え：「Aの家」と書く
住所メモ(lst):「Aの家」
Aの家: content="A", next→Bの家
Bの家: content="B"

なぜ片方はポインタのポインタでもう片方はポインタなのか
void	ft_lstadd_front(t_list **lst, t_list *new)
lst ・・リストの先頭を書き換えたいから　ー＞　ポインタのポインタ
new ・・リストのなかに「追加したいノード」だからー＞ふつうのポインタ

int x = 10;
int *p = &x;

void change(int *p)
{
    *p = 20; // pが指すxの値を変える
}

int main(void)
{
    int x = 10;
    change(&x);
}
ここでは「xの中身」を変えたいから「&x」を渡しています。
つまり、

“中身を変えたいもの”の「アドレス」を渡す
「値を使う」とき　ｰ>　*(int *)node ->content
「文字列を使う」とき　ｰ> (char *)lst->content
　　　　　　　　　　　　　(char *)lst->next->content
